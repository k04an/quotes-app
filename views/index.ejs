<html>
    <head>
        <title>Simple quotes app</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,500;0,700;1,500&display=swap" rel="stylesheet"> 
        <style>
            * {
                margin: 0;
                font-family: 'Open Sans', sans-serif;
                color: white;
                overflow: hidden;
            }

            .wrapper {
                height: 100%;
                width: 100%;
                position: relative;
                z-index: -1;
                display: flex;
            }

            .bg {
                background: rgb(2,0,36);
                background: linear-gradient(165deg, rgba(2,0,36,1) 30%, <%= gradient %> 100%);
                height: 100%;
                width: 100%;
                position: absolute;
                filter: brightness(0.5);
                animation: bg-bright 1s ease-in-out 0.5s 1 normal forwards;
            }

            .content {
                z-index: 1;
                height: 100%;
                width: 100%;
                display: flex;
                justify-content: center;
                align-items: center;
            }

            .credits {
                background-color: #212A3E;
                width: 300px;
                position: absolute;
                right: 50px;
                bottom: 0px;
                padding: 7px;
                border-top-left-radius: 10px;
                border-top-right-radius: 10px;
                text-align: center;
            }

            a {
                text-decoration: none;
            }

            @keyframes bg-bright {
                from {
                    filter: brightness(0.5);
                }

                to {
                    filter: brightness(1);
                }
            }
        </style>
    </head>

    <body>
        <div class="wrapper">
            <div class="content">
                <div style="padding: 350px; margin-top: -170px;">
                    <h1 style="text-align: center; margin-bottom: 20px;">Here's your random quote, <span style="color: <%= appealColor %>;"><%= appeal %></span></h1>
                    <p style="margin-bottom: 15px;"><%= quote.q %></p>
                    <p style="text-align: end; font-style: italic;">- <%= quote.a %></p>
                </div>
            </div>


            <div class="bg"></div>
        </div>

        <div class="credits">API by <a style="color: <%= appealColor %>;" href="https://zenquotes.io">Zenquotes</a></div>
    </body>
</html>